# 2026-05-25 - CSRC-020

Implemented the first durable route continuity slice.

## Changes

- Added a provider-opaque session route affinity ledger.
- Persisted route affinity on successful provider endpoint selection.
- Restored persisted affinity when `ProxyState` is recreated.
- Preserved TTL pruning for restored entries.
- Added a proxy restart regression for `/responses/compact`.

## Validation

- `cargo nextest run -p codex-helper-core session_route_affinity --no-fail-fast`
- `cargo nextest run -p codex-helper-core proxy_restores_route_affinity_after_restart_for_responses_compact --no-fail-fast`

Both passed on 2026-05-25.

## Follow-Up

CSRC-030 should define explicit continuity decisions for compact failure:
non-state-bound compact may fallback, while provider-state-bound compact should
not silently switch provider endpoints.
