# 2026-05-26 - CCFH-020

Release boundary fix:

- Root workspace now has `default-members = ["."]`.
- Root package has explicit `[package.metadata.dist] dist = true`.
- Tauri desktop package has explicit `[package.metadata.dist] dist = false`.
- `dist-workspace.toml` now sets `precise-builds = true`, which makes cargo-dist invoke cargo with
  `--package=codex-helper` instead of `--workspace`.

Validation:

- `dist generate --mode=ci --check` passed.
- `dist host --steps=create --tag=v0.17.0 --output-format=json > plan-dist-manifest.json` passed
  and listed only CLI release artifacts.
- `dist build --artifacts=local --target=x86_64-pc-windows-msvc --output-format=json >
  dist-build-local.json` passed and no longer compiled `codex-helper-desktop`.
