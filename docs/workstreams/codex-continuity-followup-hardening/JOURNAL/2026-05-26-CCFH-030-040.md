# 2026-05-26 - CCFH-030 / CCFH-040

Topology helper:

- Added `RoutePlanContinuityTopology` under `routing_ir`.
- Replaced duplicate configured-endpoint counting in HTTP provider execution and Responses
  WebSocket selection.
- Replaced relay capability diagnostics' ad hoc selected-domain counting with the shared helper.
- Added a routing IR unit test for endpoint count, explicit-domain count, provider-endpoint
  fallback, and missing endpoint lookup.

Response semantics split:

- `response_semantics.rs` now keeps models, streaming, generic retry/failover, response fixing,
  stale previous-response repair, session completion, and service-tier tests.
- `response_semantics_compact.rs` owns compact, state-bound continuity, route affinity, and request
  content-encoding tests.
- `response_semantics_websocket.rs` owns Responses WebSocket route, frame, and compact-trigger
  tests.

Validation:

- `cargo fmt --all --check` passed.
- `cargo nextest run -p codex-helper-core continuity_topology continuity_domain route_affinity
  capabilities --no-fail-fast` passed: 38 tests run, 38 passed.
- `cargo nextest run -p codex-helper-core response_semantics remote_compaction_v2
  responses_websocket --no-fail-fast` passed: 70 tests run, 70 passed.
