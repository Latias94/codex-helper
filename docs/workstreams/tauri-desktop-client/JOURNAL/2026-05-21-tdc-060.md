# 2026-05-21 — TDC-060 Read-Only Admin API Wiring

## What changed

- Added a Tauri read-model command, `get_admin_read_model`, that fetches the local admin API without relying on browser CORS.
- Added frontend admin DTOs, mapping functions, TanStack Query hooks, mock fallback data, and state banners.
- Wired Dashboard, Providers, Usage, Settings, StatusStrip, sidebar runtime card, and page header status badges to live data when available.
- Kept mock fallback explicit and visible for design iteration or disconnected local runtime.

## Validation

- `pnpm test` in `apps/desktop`: PASS, 3 files / 9 tests.
- `pnpm build` in `apps/desktop`: PASS.
- `cargo fmt --check`: PASS.
- `cargo check -p codex-helper-desktop`: PASS.
- `git diff --check -- .`: PASS.
- Loopback admin smoke:
  - `Test-NetConnection 127.0.0.1:4211`: PASS.
  - `GET /__codex_helper/api/v1/operator/summary`: PASS, returned API v1 operator summary and links.

## Concerns

- Full Tauri window smoke is still pending.
- TDC-070 should split disconnected, auth-token-required, stale, loading, and empty states into more precise UI variants.
- Provider credentials remain read-only placeholders because this slice deliberately avoids secret material and mutations.
