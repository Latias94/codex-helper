# TDC-100/TDC-110 Replacement Readiness And Closeout

Date: 2026-05-22

## Summary

- Added `REPLACEMENT_READINESS.md` as the replacement-readiness authority for the Tauri desktop client.
- Documented that `apps/desktop` is the preferred long-term GUI replacement path, but not yet ready to replace the released egui GUI or delete `crates/gui`.
- Split remaining desktop parity work into follow-ons:
  - packaged sidecar and installer behavior;
  - single instance;
  - launch at login;
  - signing and auto-update;
  - lightweight single-config import/export;
  - open config/log/cache paths;
  - provider edit parity;
  - full packaged tray/window lifecycle smoke;
  - egui deprecation/removal.
- Recorded the explicit import/export boundary: one primary config file, validation and backup, no heavy profile/workspace/config-catalog layer.

## Closeout Position

The workstream can close with follow-ups because the original lane has reached a clear replacement-readiness boundary:

- product direction accepted;
- production Tauri app scaffolded;
- simplified MVP pages implemented;
- live read model wired;
- safe control actions wired;
- close-to-tray lifecycle semantics implemented and smoke-tested;
- remaining parity gaps are explicit and split.

The lane must not be interpreted as an egui-removal approval.

## Validation

- `pnpm test` in `apps/desktop`: PASS — 5 files, 22 tests.
- `pnpm build` in `apps/desktop`: PASS.
- `cargo fmt --check`: PASS.
- `cargo check -p codex-helper-desktop`: PASS.
- `cargo nextest run -p codex-helper-desktop --lib`: PASS — 9 tests.
- `git diff --check -- .`: PASS, only Windows LF/CRLF warnings.

Final evidence is recorded in `EVIDENCE_AND_GATES.md`.
