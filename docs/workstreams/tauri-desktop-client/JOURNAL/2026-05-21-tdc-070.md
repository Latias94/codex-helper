# TDC-070 Runtime Data States

Date: 2026-05-21

## Summary

- Added a shared runtime data-state classifier for the desktop frontend.
- Replaced the generic fallback banner with state-aware UI for loading, unavailable, disconnected, auth-required, empty, refreshing, and stale states.
- Centralized the admin read-model TanStack Query boundary so pages and shell chrome agree on runtime state.
- Surfaced lifecycle owner uncertainty as explicit UI copy rather than hiding it.

## Validation

- `pnpm test` in `apps/desktop`: PASS — 5 files, 20 tests.
- `pnpm build` in `apps/desktop`: PASS.
- `cargo fmt --check`: PASS.
- `cargo check -p codex-helper-desktop`: PASS.
- `git diff --check -- .`: PASS, only Windows LF/CRLF warnings.

## Concerns

- Full Tauri window visual smoke remains pending.
- Safe mutations are intentionally placeholder/disabled until TDC-080.
- Authoritative owner semantics remain TDC-090.
