# 2026-05-20 — CAD-020 Session Identity Semantics

## Work Completed

- Added explicit `SessionIdentitySource` values for header-derived and `prompt_cache_key`-derived identities.
- Introduced `ClientSessionIdentity { value, source }` for Codex HTTP and Responses WebSocket preparation.
- Threaded optional source metadata through request logs, split debug logs, active/finished request state, request-log replay, session stats/cards, and route affinity records.
- Preserved routing behavior: existing `session_id` string remains the key for bindings, overrides, route selection, and affinity.
- Added/strengthened characterization tests for header priority, prompt-cache fallback, request-log serialization, session card source merge, and response-semantics affinity observability.

## Verification

- `cargo fmt --check` — PASS
- `cargo nextest run -p codex-helper-core request_log_serializes_request_id_when_present request_log_serializes_codex_bridge_metadata extract_session_id_uses_prompt_cache_key_body_fallback extract_session_id_prefers_headers_over_prompt_cache_key build_session_identity_cards_merges_sources_and_sorts_newest_first --no-fail-fast` — PASS, 5 passed
- `cargo nextest run -p codex-helper-core prompt_cache_key_affinity --no-fail-fast` — PASS, 1 passed
- `cargo nextest run -p codex-helper-core response_semantics --no-fail-fast` — PASS, 20 passed

## Notes For CAD-030

- HTTP and Responses WebSocket now use the same identity vocabulary, which should make shared request preparation extraction cleaner.
- Watch for duplication around config reload/snapshot, session binding/override resolution, `prepare_request_body`, `begin_request`, and route selection.
