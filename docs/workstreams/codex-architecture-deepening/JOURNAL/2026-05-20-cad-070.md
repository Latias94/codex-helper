# 2026-05-20 — CAD-070 Verification And Closeout

## Scope

- workspace verification
- `crates/core/src/state.rs`
- workstream closeout docs

## What happened

- Ran final formatting and package tests.
- The first full package run found a real closeout regression:
  - failing test: `state::tests::finish_request_estimates_cost_and_rolls_up_cost`;
  - symptom: request cost was unknown (`None`) instead of `Some("0.0061375")`.
- Diagnosed the issue to a test-only positional argument drift:
  - CAD-020 added `session_identity_source` to `ProxyState::begin_request`;
  - several state tests inserted the new `None` at the wrong position;
  - the cost test shifted `"gpt-5"` from `model` to `cwd`, so pricing had no model.
- Fixed the state test call sites and reran both narrowed and full gates.

## Validation

```powershell
cargo nextest run -p codex-helper-core state::tests::finish_request_estimates_cost_and_rolls_up_cost begin_and_finish_requests_keep_trace_id usage_rollup_view_scores_entities_inside_selected_window --no-fail-fast
# PASS — 3 passed, 597 skipped

cargo nextest run -p codex-helper-core
# PASS — 600 passed, 0 skipped

cargo fmt --check
# PASS
```

## Closeout decision

- Workstream status set to complete.
- No required follow-on split.
- Optional future hardening:
  - add typed test builders for `begin_request` to avoid positional drift;
  - selectively migrate more proxy integration tests to `proxy/tests/harness.rs` when doing adjacent test work.

## Commit status

Not committed. Ask the user before staging/committing.
