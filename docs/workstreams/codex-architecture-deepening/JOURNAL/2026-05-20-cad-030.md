# 2026-05-20 — CAD-030 Shared Request Preparation

## Work Completed

- Added `RequestConfigContext`, `CommonRequestPreparationParams`, and `CommonPreparedRequest` in `crates/core/src/proxy/request_preparation.rs`.
- Moved duplicated shared flow into `prepare_common_request`:
  - config snapshot use after reload,
  - session identity extraction,
  - default session binding and session touch,
  - manual overrides and default-profile body rewrite,
  - request body preview setup,
  - begin-request,
  - retry plan/cooldown backoff,
  - route request context and route selection.
- Kept HTTP-specific responsibilities in `request_context.rs`:
  - body read limit,
  - content-encoding normalization,
  - request flavor detection,
  - early body/decode/no-route error logging.
- Kept WebSocket-specific responsibilities in `responses_websocket.rs`:
  - first `response.create` message validation,
  - WebSocket handshake and beta header setup,
  - frame body replacement.
- Added `prepare_common_request_tracks_prompt_cache_identity_and_overrides_body` to characterize shared preparation across prompt-cache identity, default-profile body rewrite, route selection, and active request state.

## Verification

- `cargo fmt --check` — PASS
- `cargo nextest run -p codex-helper-core prepare_common_request_tracks_prompt_cache_identity_and_overrides_body --no-fail-fast` — PASS, 1 passed
- `cargo nextest run -p codex-helper-core request_content_encoding prompt_cache_key_affinity responses_websocket --no-fail-fast` — PASS, 18 passed
- `cargo nextest run -p codex-helper-core response_semantics --no-fail-fast` — PASS, 20 passed

## Notes For CAD-040

- The next task should be independent of CAD-030 production paths: relay capability/live-smoke diagnostics.
- Preserve existing evidence schema and explicit ACK behavior while extracting cases into a registry.
