# 2026-05-31 - Closeout

Completed the runtime boundary refactor:

- introduced `ProxyRuntimeOptions` for admin bind, advertised admin URL, and host-local policy;
- moved host-local capability reporting to runtime-local `ProxyService` state;
- moved server config merge and validation into `EffectiveServerConfig`;
- added advertised admin URL support for server deployments;
- split local CLI server startup into local client preparation and proxy runtime construction helpers;
- repaired Docker cargo-chef and entrypoint behavior.

Fresh gates passed:

- `cargo fmt --all -- --check`
- `cargo check --locked -p codex-helper`
- `cargo check --locked -p codex-helper-server`
- `cargo test --locked -p codex-helper-server --no-fail-fast`
- `cargo nextest run --locked -p codex-helper-core host_local admin_discovery proxy_api_v1_capabilities_and_overrides_work --no-fail-fast`
- `docker build --target runtime -t codex-helper:local .`
- `docker run --rm codex-helper:local --help`
- `docker compose --env-file deploy/compose/.env.example -f deploy/compose/codex-helper.yml config --quiet`
