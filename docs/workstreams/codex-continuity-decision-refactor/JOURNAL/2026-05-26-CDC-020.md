# CDC-020 Journal

Date: 2026-05-26
Status: DONE

## Work Completed

- Added `crates/core/src/proxy/request_continuity.rs`.
- Moved HTTP remote-compaction body classification behind `classify_request_continuity`.
- Moved route execution continuity decision behind `route_continuity_decision`.
- Classified Responses WebSocket first `response.create` frames, including `compaction_trigger`.
- Marked WebSocket v2 compact requests in `CodexBridgeLog` without changing fallback behavior.

## Validation

```powershell
cargo nextest run -p codex-helper-core remote_compaction_v2 responses_websocket route_affinity --no-fail-fast
cargo fmt --all --check
cargo nextest run -p codex-helper-core request_continuity request_flavor_finalizes_remote_compaction_v2_from_body responses_websocket_relays_headers_model_mapping_and_frames route_graph_policy_treats_remote_compaction_v2_as_state_bound --no-fail-fast
```

All commands passed.

## Follow-Up

CDC-030 should wire the WebSocket compact classification into state-bound target selection. This journal intentionally did not relax fallback behavior.
