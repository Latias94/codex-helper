# 2026-05-31 - Closeout

The relay target workflow is complete.

Delivered:

- added persisted `relay_targets` config with version migration preservation;
- added shared relay target resolution and control-plane admin client primitives;
- added `ch relay add/list/status/off/use` plus `ch relay <target>` shorthand;
- kept plain `ch`, `serve`, and `switch` local behavior compatible;
- refactored attached TUI to consume a resolved admin base URL and optional admin token env var;
- documented local, NAS, switch-only, and attach-only workflows in README, configuration docs, Docker Compose docs, and the generated config template.

Validation:

- `cargo fmt --all -- --check`
- `cargo check --locked -p codex-helper-core`
- `cargo check --locked -p codex-helper-tui`
- `cargo check --locked -p codex-helper`
- `cargo nextest run --locked -p codex-helper-core relay_target control_plane --no-fail-fast`
- `cargo nextest run --locked -p codex-helper-core v2_schema v4_schema --no-fail-fast`
- `cargo nextest run --locked -p codex-helper relay_cli --no-fail-fast`
- `cargo nextest run --locked -p codex-helper-tui attached --no-fail-fast`
- `docker compose --env-file deploy/compose/.env.example -f deploy/compose/codex-helper.yml config --quiet`
- `git diff --check`

Follow-on:

- Remote attached TUI still uses local config to build provider option labels. Split full remote provider editing into a dedicated control-plane/TUI lane instead of extending this workflow.
