# 2026-05-19 Start

Opened workstream after source review of `repo-ref/codex` and `repo-ref/sub2api`.

Decision: the correct implementation is a helper-owned WebSocket relay with an explicit transport switch, not extra patch-mode combinations. Direct Codex-to-sub2api is useful as a manual validation path but is not the product
solution because it bypasses helper routing, filtering, observability, and auth policy.

