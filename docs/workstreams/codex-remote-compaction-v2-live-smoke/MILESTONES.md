# Milestones

- [x] `remote_compaction_v2` appears as an explicit live-smoke case.
- [x] CLI/API callers can request it without changing default compact smoke behavior.
- [x] Evidence records whether v2 stream shape was actually observed.
