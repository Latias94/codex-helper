2026-05-28
- Opened workstream for the user-approved fearless refactor goal.
- First executable task is CFR-020: shared bounded local log storage, including control_trace.
- Completed CFR-020. Added `local_log_store`, removed bespoke runtime/request rotation code, bounded GUI logs and relay evidence, and documented retention knobs.
- Validation passed: `cargo fmt --check`, `cargo nextest run -p codex-helper-core local_log_store codex_relay_evidence logging --no-fail-fast`, `cargo nextest run -p codex-helper --no-fail-fast`, and `cargo check -p codex-helper-gui`.
- Completed CFR-030. Added semantic routing authoring helpers and removed high-level manual compat-sync call sites from CLI, GUI, and admin surfaces.
- Validation passed: `cargo nextest run -p codex-helper-core config route routing --no-fail-fast`, `cargo nextest run -p codex-helper --no-fail-fast`, `cargo check -p codex-helper-gui`, and `cargo fmt --check`.
- Completed CFR-040. Added `RequestLedgerStore`, routed CLI/TUI/GUI/admin consumers through it, and made recent/filter reads streaming bounded-window operations.
- Validation passed: `cargo nextest run -p codex-helper-core request_ledger logging --no-fail-fast`, `cargo nextest run -p codex-helper --no-fail-fast`, `cargo check -p codex-helper-gui`, and `cargo fmt --check`.
- Completed CFR-050. Split live-smoke case descriptors, HTTP specs, and request bodies into `codex_relay_live_smoke/cases.rs`.
- Completed CFR-060 closeout. Changelog and evidence updated; workstream status is complete.
- Validation passed: `cargo nextest run -p codex-helper-core relay_live_smoke codex_live_smoke --no-fail-fast`, `cargo nextest run -p codex-helper --no-fail-fast`, `cargo check -p codex-helper-gui`, and `cargo fmt --check`.
