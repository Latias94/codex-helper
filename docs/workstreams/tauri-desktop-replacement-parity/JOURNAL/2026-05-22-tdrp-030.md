# TDRP-030 Single Instance

Date: 2026-05-22

## Summary

- Added `tauri-plugin-single-instance`.
- Registered a second-instance callback in the Tauri builder.
- Callback reuses `lifecycle::show_main_window`, so a second launch shows, unminimizes, and focuses the existing main window.
- Added a lifecycle policy test proving second-instance launch has no proxy shutdown effect.

## Validation

- `cargo check -p codex-helper-desktop`: PASS.
- `cargo nextest run -p codex-helper-desktop --lib lifecycle::tests::second_instance_launch_never_stops_proxy_runtime`: PASS.
- `pnpm test`: PASS — 5 files, 23 tests.
- `pnpm build`: PASS.
- `cargo fmt --check`: PASS.
- `cargo nextest run -p codex-helper-desktop --lib`: PASS — 13 tests.
- `git diff --check -- .`: PASS, only Windows LF/CRLF warnings.

## Concerns

- Real packaged second-launch behavior still needs TDRP-080 smoke evidence before egui replacement.
